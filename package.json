{
  "name": "workshop-ts-exercises",
  "version": "1.0.0",
  "description": "TypeScript basics workshop – Music Viewer App (Exercises)",
  "scripts": {
    "build": "tsc && sass --no-source-map src/styles.scss public/styles.css",
    "start": "node server.js",
    "dev": "tsc && sass --no-source-map src/styles.scss public/styles.css && node server.js"
  },
  "devDependencies": {
    "sass": "^1.98.0",
    "typescript": "^5.4.0"
  }
}
